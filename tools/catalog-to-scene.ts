// catalog-to-scene — a BUILD-TIME content tool (never shipped in any wasm).
// Reads catalog/parts/*.json and a blueprint, emits a Quine scene.json the
// engine can render. Analogous to quine's apps/desktop/worlds.zig generators:
// content authoring lives outside the engine.
//
// TODO(Phase 3/7): implement.

export {};
