{
  "name": "qubekit",
  "private": true,
  "version": "0.0.0",
  "description": "QubeKit — a programmable mechanical construction environment for Qubeworlds.",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck"
  }
}
