{
  "name": "qubekit",
  "private": true,
  "description": "QubeKit — a collaborative multiplayer mechanical construction kit on the Qubeworlds stack.",
  "license": "MIT OR Apache-2.0",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "setup": "./init.sh"
  }
}
