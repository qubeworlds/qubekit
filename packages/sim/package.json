{
  "name": "@qubekit/sim",
  "version": "0.0.0",
  "private": true,
  "description": "Portable authoritative world logic — imported by the qubepods stateful runtime.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@qubekit/schema": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
