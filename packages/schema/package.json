{
  "name": "@qubekit/schema",
  "version": "0.0.0",
  "private": true,
  "description": "Shared types + validators for QubeKit parts, assemblies, and construction ops.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "echo \"no tests yet\""
  }
}
