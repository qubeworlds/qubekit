{
  "name": "@qubekit/client",
  "version": "0.0.0",
  "private": true,
  "description": "Construction client: snap resolution, optimistic apply, qubegame transport glue.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json || true",
    "test": "echo \"no tests yet\""
  }
}
